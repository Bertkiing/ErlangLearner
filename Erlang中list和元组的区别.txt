Erlang中元组和列表的区别和联系：
区别：
    1.列表中的元素个数是可变的，而元组的数目在创建后就固定了.
    2.创建的方式不同,列表是使用[],而元组是{}.
    3.取值的方式不同：对于从元组中提取字段值，一般使用的是“=”来提取；而对于列表来讲
通过每次提取这个列表的头部，也就是每次只能提取一个项。总之，两者都是使用的模式匹配。
    4.在函数使用上的区别。
        1)当函数返回值为元组或列表，且其中的元素个数不确定时，最好使用一个变量来接收(去掉{});
        2)而返回值的元素只有一个时，要使用其对应的表示形式({},[]).